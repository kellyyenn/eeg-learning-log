# eeg-learning-log

Learning log for working with EEG data.

## Setup

```bash
# Create and activate the venv
python3.11 -m venv .venv
source .venv/bin/activate

# Install dependencies
pip install numpy jupyter matplotlib
```

## Running Jupyter

```bash
source .venv/bin/activate
jupyter lab
```
